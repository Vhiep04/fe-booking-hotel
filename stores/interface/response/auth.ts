export interface LoginResponseData {
  token: string;
  firstName: string;
  lastName: string;
  avatarUrl: string;
  email: string;
  phoneNumber: string;
}
export interface RegisterResponseData {
  firstName: string;
  lastName: string;
  email: string;
}
